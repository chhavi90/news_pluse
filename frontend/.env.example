# Base URL of the News Pulse backend API (no trailing slash).
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:4000
# Production (example): NEXT_PUBLIC_API_URL=https://news-pulse-api.onrender.com
